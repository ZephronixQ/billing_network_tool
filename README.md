# Unregistered ONU Scanner via Telnet

Этот Python скрипт позволяет автоматически собирать информацию о незарегистрированных ONU (Optical Network Units) на GPON коммутаторах через Telnet и выводить результаты в удобной табличной форме.

---

## 📋 Описание

Скрипт подключается к списку коммутаторов, выполняет команды для отображения незарегистрированных ONU и собирает следующие данные:

- IP коммутатора
- Порт GPON (`x/x/x`)
- Серийные номера незарегистрированных ONU
- Общее количество незарегистрированных ONU на порту и коммутаторе

Результаты выводятся в консоль в виде цветной таблицы с суммарными данными по каждому коммутатору.

---

## ⚙️ Настройки

В начале скрипта определены основные настройки:

```python
SWITCHES: List[str] = [
    "192.168.2.12",
    "192.168.2.13",
    ...
]

USERNAME: str = "admin"          # Логин Telnet
PASSWORD: str = "admin"          # Пароль Telnet
TELNET_PORT: int = 23            # Порт Telnet
SEM: asyncio.Semaphore = asyncio.Semaphore(len(SWITCHES))  # Контроль параллельных подключений
```

> **Примечание:** Для корректной работы скрипта необходимо заменить логин и пароль на ваши данные.

---

## 🛠️ Требования

* Python 3.10+
* Библиотеки Python:

```bash
pip install asyncio telnetlib3 tabulate
```

---

## 🚀 Использование

Запустить скрипт можно напрямую:

```bash
python unregistered_onu.py
```

Пример вывода:

```
📊 Total unregistered ONU: 5
┏━━━━━━━━━━━━━━┳━━━━━━━━━┳━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━┓
┃     IP       ┃  PORT   ┃   SERIALS    ┃ UNCFG ONU    ┃
┡━━━━━━━━━━━━━━╇━━━━━━━━━╇━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━┩
│ 179.168.2.12 │ 1/1/1   │ 12345678AB   │ 1            │
│ 192.168.2.12 │ 1/1/2   │ 87654321CD   │ 1            │
└──────────────┴─────────┴──────────────┴──────────────┘

📌 Summary per switch:
192.168.2.12 -> Total unregistered ONU: 2
192.168.2.13 -> Total unregistered ONU: 3
```

* Зеленый цвет — IP коммутатора
* Желтый цвет — порт GPON
* Сиреневый — серийные номера ONU
* Голубой/бирюзовый — количество незарегистрированных ONU

---

## ⚡ Основные функции

1. **Telnet-подключение с ретраями:**
   функция `telnet_connect` обеспечивает надежное подключение к каждому коммутатору с попытками повторного подключения.

3. **Отправка команд и чтение вывода:**
   `send_command` отправляет команды на коммутатор и собирает весь вывод, обрабатывая ANSI-коды и лишние символы.

4. **Парсинг незарегистрированных ONU:**
   `parse_uncfg_onu` извлекает порт и серийный номер каждой незарегистрированной ONU.

5. **Асинхронная обработка нескольких коммутаторов:**
   используется `asyncio` и семафор для параллельного подключения к всем коммутаторам без перегрузки.

6. **Вывод данных в табличном виде:**
   `print_table_with_uncfg_count` формирует красивую таблицу с цветами и подсчетом ONU.
